<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.work.prepmaster">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/mortarboard"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mortarboard"
        android:supportsRtl="true"
        android:theme="@style/SpecialActionBar">
        <activity android:name=".ReminderActivity"></activity>
        <activity android:name=".Dictionary" />
        <activity android:name=".StatisticActivity" />
        <activity android:name=".Register2Activity" />
        <activity android:name=".Register1Activity" />
        <activity android:name=".ProfileOptionsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".PracticeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>